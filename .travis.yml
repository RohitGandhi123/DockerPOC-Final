sudo: true
dist: trusty

env:
  global:
    - $IMAGE_NAME: dockerpoc/devops
    - $DOCKER_USERNAME: gandhir26
    - $DOCKER_PASSWORD: Microsoft@123
   
jobs:
  include:
    - stage: build docker image
      script:
      - docker build -t docker.io:gandhir26/dockerpoc/devops .
      - docker images
      - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
      - docker push $DOCKER_USERNAME/${IMAGE_NAME}
    - stage: test
      script: docker run --rm $DOCKER_USERNAME/${BASE_IMAGE} cat hello.txt
    - script: docker run --rm $DOCKER_USERNAME/${BASE_IMAGE} cat hello.txt
